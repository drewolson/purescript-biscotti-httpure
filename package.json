{
  "description": "purescript-httpure-contrib-biscotti",
  "repository": "https://github.com/drewolson/purescript-httpure-contrib-biscotti",
  "license": "MIT",
  "scripts": {
    "ci": "npm run test && npm run format:check",
    "test": "spago test",
    "format": "find src test -name \"*.purs\" | xargs -n1 purty format --write",
    "format:check": "find src test -name \"*.purs\" | xargs -n1 purty validate"
  },
  "devDependencies": {
    "purescript-language-server": "^0.12.7",
    "purty": "^6.2.0",
    "uuid": "^3.3.3",
    "uuid-validate": "0.0.3"
  }
}
